[pytest]
asyncio_mode = auto
testpaths = api/tests
markers =
    postgres: tests that require PostgreSQL
    elasticsearch: tests that require Elasticsearch
    integration: integration tests using external services
